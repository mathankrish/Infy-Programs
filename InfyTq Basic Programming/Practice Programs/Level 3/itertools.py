# from itertools import accumulate
# l1 = [1,2,3,4,5]
# print(list(accumulate(l1, lambda x, y: x+y)))


# from itertools import chain
# l1 = ["abc", "asd"]
# print(list(chain.from_iterable(l1)))
# print(list(chain("asdf")))


# from itertools import compress
#
# print(list(compress('asddff', [0, 1, 0, 1, 1, 1])))

# from itertools import groupby
# p = [k for k, g in groupby('aaaacccvvvvbbbaaa')]
# p1 = [len(list(g)) for k, g in groupby('aaaacccvvvvbbb')]
#
# print(p)
# print(p1)


